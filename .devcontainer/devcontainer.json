{
  "name": "granzer04.gpu_facts",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "redhat.ansible"
      ]
    }
  },
  "postCreateCommand": "python -m pip install --upgrade pip; pip install -r requirements-dev.txt",
  "remoteUser": "vscode"
}
